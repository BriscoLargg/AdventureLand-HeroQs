import { Action } from "HeroQs/Actions/Action";

describe("Action", () => {
    describe("has delegate ", () => {
        it("that can be Invoked", () => {
            
            let action = new Action();
          });
    });
  });
